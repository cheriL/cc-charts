apiVersion: v1
kind: Service
metadata:
  name: {{ include "cc-charts.fullname" . }}
  labels:
    {{- include "cc-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cc-charts.selectorLabels" . | nindent 4 }}
